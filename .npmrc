# Ensure consistent behavior across platforms
package-lock=true
save-exact=true
fund=false
audit=false

# Use consistent lockfile version
lockfile-version=3

# Ensure consistent dependency resolution
legacy-peer-deps=false
